---
- name: Backup Cisco IOS running configuration
  hosts: all
  gather_facts: false
  connection: network_cli
  tasks:
    - name: Backup running config
      cisco.ios.ios_config:
        backup: true
        backup_options:
          filename: router1-bk.cfg
          dir_path:
      register: config_backup
